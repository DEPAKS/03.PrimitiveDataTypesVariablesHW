using System;

class VariableInHexadecimalFormat
{
    static void Main()
    {
        int hexDecimalNumber = 0xFE;

        Console.WriteLine(hexDecimalNumber);
    }
}

// P03: Declare an integer variable and assign it with the value 254 in hexadecimal format (0x##). Use Windows Calculator to find its hexadecimal representation. Print the variable and ensure that the result is “254”.